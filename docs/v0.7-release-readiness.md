# v0.7 release readiness review

Review basis: release-candidate commit `e5d97bc` (`e5d97bc17d1a7355bec703780c6b2eca07cf42cf`)
and GitHub Actions CI run `36070675212`. The exact pushed candidate passed the
Python 3.11, 3.12, 3.13, and 3.14 test jobs and the package job. This document
records the release-gate decision against
[`v0.7-developer-product.md`](v0.7-developer-product.md), section 19. It is
release-readiness evidence; it is not evidence of a tag or published package.

## Evidence available

| Gate group | Evidence and status |
| --- | --- |
| C1-C6 correctness | **PASS.** Facade tests cover dispatch reservation, recovery, eligibility, replay, verification, identity/idempotency behavior, and observation-only reconciliation with retained Warrant lineage. The architecture mapping is documented in the [public API](v0.7-public-api.md). Final review of `e5d97bc` found the facade orchestrates the existing core contracts rather than introducing a duplicate semantic engine, and the exact-candidate regression suite passed in CI. |
| D2–D3 experience | Quickstart and three disposable scenarios print Decision, adapter observation, verification, Effect, and Warrant information; examples run in the installed wheel CI job. |
| D1 author rehearsal | On 2026-09-24 the project author created a fresh temporary Windows venv, installed the source checkout with `pip install -e .`, ran `examples/sqlite_delete.py` and `cage example database.delete`, and observed `ADMITTED`, `ACKNOWLEDGED`, `VERIFIED_BOUND`, and `BOUND` in both runs. Python was 3.13.2 and `[System.Environment]::OSVersion.VersionString` reported `Microsoft Windows NT 10.0.26200.0`. The reported elapsed time was 0.22 minutes. This was an author rehearsal using provided PowerShell commands, not an unfamiliar developer following the published Quickstart independently. |
| D4-D5, S1-S3 | **PASS.** Final review against `e5d97bc` confirmed that the integration and failure/recovery guides match the public adapter and verifier contracts. Adapter failures after possible dispatch retain recovery execution context with an `UNKNOWN` observation rather than triggering re-execution. Reconciliation is observation-only. SUMMARY remains the default portable-Warrant disclosure, sensitive parameters and reference arrays are omitted by default, CLI inspection avoids payload contents, and parsed portable Warrants cannot restore execution authority. |
| O3, P1–P2 CLI and portability | `cage --help`, `--version`, all three `example` commands, and Warrant inspect/validate are exercised in tests or CI. Portable format v1 and disclosure are specified in the public API document. |
| K2–K3 Windows and packaging | The package job builds a wheel and sdist, checks packaged resources, installs the wheel outside the checkout, and invokes its examples and CLI. On 2026-09-24, the final local `0.7.0` candidate wheel `cage_assurance-0.7.0-py3-none-any.whl` was installed outside the checkout into a fresh temporary venv using Python 3.13.2 on `Windows-11-10.0.26200-SP0` with `pip install --no-index --no-deps`. The run confirmed the public `CAGE` import, `cage --version` = `0.7.0`, CLI help, all three `cage example` scenarios, Warrant inspect/validate with structurally valid SUMMARY Warrants, and all four installed packaged example scripts. The run ended with `CAGE v0.7.0 WINDOWS WHEEL SMOKE TEST PASSED`. |
| K1 static compatibility | The v0.6.0 compatibility review found all 18 baseline `src/cage/core` modules still present, no removals among the 44 reviewed public top-level classes and functions, no removed or renamed baseline test functions, and no changed argument lists, defaults, or annotations among the reviewed public core functions and methods. The v0.6 package root exposed no public imports; v0.7 adds supported root imports. Final candidate `e5d97bc` changed no runtime implementation relative to `2379886`, and exact-candidate CI passed the supported Python test matrix. |
| Python CI | [Run 36070675212](https://github.com/roopamwsure/cage/actions/runs/36070675212) completed successfully for exact candidate commit `e5d97bc`. Python 3.11, 3.12, 3.13, and 3.14 test jobs passed, including the local examples. The package job built the wheel and source archive, inspected distribution contents including the six packaged guides and Apache-2.0 metadata, and installed and ran the wheel outside the source checkout. CI runners are Ubuntu; Windows evidence is recorded separately below. |
| v0.7 local candidate smoke | On 2026-09-24, Python 3.12.14 on Linux built a `0.7.0` editable install, ran the source Quickstart's SQLite delete, used CLI export/inspect/validate, built a wheel and source archive, and checked the six packaged user guides and Apache-2.0 metadata. A clean venv outside the checkout installed the wheel with no dependencies and ran access and payment scenarios with structurally valid SUMMARY exports. This is separate from the author's Windows 3.13.2 rehearsal and final Windows release-wheel check. |

## Release decision record

| Gate | Final decision |
| --- | --- |
| D1, owner-deferred | **OWNER-DEFERRED / accepted release exception.** Independent first-time-developer testing was not performed for v0.7. The author rehearsal and automated examples do not satisfy the original unfamiliar-developer gate. This gate must not be reported as passed; onboarding feedback should be revisited after release. |
| D4-D5, S1-S3 | **PASS.** Final review against `e5d97bc` confirmed that the integration and failure/recovery guides match the public adapter and verifier contracts. Adapter failures after possible dispatch retain recovery execution context with an `UNKNOWN` observation rather than triggering re-execution. Reconciliation is observation-only. SUMMARY remains the default portable-Warrant disclosure, sensitive parameters and reference arrays are omitted by default, CLI inspection avoids payload contents, and parsed portable Warrants cannot restore execution authority. |
| K1-K2 | **PASS for the tested matrix.** The v0.6 compatibility review found no removed baseline modules, public functions, methods, or baseline tests and no changed public signatures in the reviewed core surface. Candidate commit `e5d97bc` contains only release, packaging, CI, and documentation changes relative to `2379886`; no runtime implementation changed in the final candidate step. CI passed Python 3.11-3.14 on Ubuntu. The final local Windows wheel check passed on Python 3.13.2 and `Windows-11-10.0.26200-SP0`. No broader OS matrix is claimed. |
| L1 | **PASS.** The required v0.7 documentation exists, including Quickstart, reconciliation, semantic, integration, failure/recovery, portable-Warrant/CLI, and public API material. The final package job verified the packaged documentation, while the source and Windows wheel rehearsals exercised the CLI, flagship examples, Warrant inspection, validation, and packaged example scripts against the `0.7.0` candidate. |
| API contract | **PASS.** Known predecessor record-ID collisions raise `IdentityConflictError`, which is a `CAGEValueError`. The behavior is documented in the public API and covered by facade evaluation tests. Invalid identity overrides and idempotency conflicts retain their documented error categories. |
| L2 | **TECHNICAL CANDIDATE GO, with D1 explicitly deferred.** Package metadata and changelog declare `0.7.0`; Apache-2.0 is present; runtime dependencies remain empty; the local regression suite and Windows release-wheel smoke passed; and exact candidate CI run `36070675212` passed all Python and package jobs for `e5d97bc`. This readiness-record update is documentation-only. Its exact pushed commit must receive green CI before creating the `v0.7.0` tag. Tagging and package publication remain separate actions. |

Next practical sequence: commit and push this documentation-only readiness
record, verify GitHub Actions on that exact commit, and confirm a clean
repository status. If that final CI run is green, the candidate may be tagged
`v0.7.0`. PyPI publication remains a separate action. The deferred D1
independent onboarding check must not be reported as passed.
